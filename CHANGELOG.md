# 📝 更新紀錄 (CHANGELOG)

---

## [v2.6.0] - 2026-07-31

### 🎨 v2.6.0 重大升級：深紫藍現代 UI 美化、開機啟動批次停用與排程彈性顯示

- **全新深紫藍主題視覺美化**：
  - 背景採用深邃藍黑 (`#0F0F17`)、側邊欄 (`#13131E`) 與卡片區塊 (`#1A1A28`)。
  - 主色調升級為霓虹藍 (`#5B7BFE`)，搭配翠綠標籤 (`#2ECC8A`) 與警告鮮紅 (`#FF4757`)。
- **圖示對齊與半型空格修正**：
  - 修正複合 emoji 寬度導致側邊欄按鈕文字對齊偏差問題（如 `🗑` 按鈕對齊）。
- **開機啟動左側勾選框與批次停用**：
  - 新增啟動項目左側勾選框與 **`🚫 批次停用所有勾選的開機啟動項`** 功能。
  - 停用後項目依然保留在清單中（標示 `🛑 [已關閉]`），隨時可點擊復原。
- **工作排程器彈性顯示開關**：
  - 預設隱藏系統自動更新等背景排程任務，並提供 **`📅 同時顯示工作排程器任務`** 勾選框供使用者自主切換。

---

## [v2.5.0] - 2026-07-31

### 🎛️ Windows 工作排程器整合與動態開關控制

- **完整整合 Windows 工作排程器 (Schtasks)**：
  - 自動讀取非微軟核心排程任務 (如 Google Update, Edge Update, Adobe Update 等)。
- **動態開關控制功能**：
  - 運行中項目提供 **`🚫 停用 / 關閉開機啟動`** 按鈕；已停用項目提供 **`✅ 復原 / 開啟開機啟動`** 按鈕。
  - 支援登錄檔 (`Run` <-> `RunDisabled`)、Startup 資料夾 (`.lnk` <-> `.lnk.disabled`) 與工作排程 (`/Disable` <-> `/Enable`) 雙向復原機制。

---

## [v2.4.0] - 2026-07-31

### 🔤 啟動項目友善繁體中文翻譯引擎

- **英文與代碼登錄檔鍵值自動解析**：
  - 調用 Win32 `GetFileVersionInfoW` 讀取執行檔語系與版本資訊 (ProductName / FileDescription)。
  - 對照內建常用軟體資料庫，將難懂的 Registry 鍵值轉換為友善繁體中文名稱與開機用途說明。

---

## [v2.3.0] - 2026-07-31

### 🔍 Windows 系統開機啟動自動偵測

- **盤點本機開機啟動項目**：
  - 自動掃描 `HKCU` / `HKLM` 登錄檔啟動區與使用者 `Startup` 資料夾。
  - 自動分析效能影響並標示 `🔴 高影響` / `🟡 中影響` / `🟢 低影響` 標籤。

---

## [v2.2.0] - 2026-07-31

### 🧱 頁面功能獨立化與介面重構

- **隔離獨立動作按鈕**：
  - 將清理執行按鈕與模擬預覽開關獨立至分頁 1 專屬區域。
- **底部全域狀態列重構**：
  - 底部改為專屬全域狀態列，即時顯示累計清理暫存容量與釋放記憶體容量。

---

## [v2.1.0] - 2026-07-31

### 🗑️ Geek Uninstaller 風格軟體徹底卸載升級

- **本機安裝軟體詳細資訊盤點**：
  - 顯示軟體圖示標籤 (`📦 [應用軟體]` / `🛡️ [系統必備元件]`)、安裝日期與估算容量。
- **關鍵字搜尋與安全保護開關**：
  - 新增即時關鍵字過濾搜尋列與 `☑️ 隱藏系統必備元件 (防誤刪)` 勾選框。
- **深層殘掃蕩**：
  - 官方卸載後自動掃描 `AppData\Local`, `AppData\Roaming`, `C:\ProgramData` 殘留資料夾並提供一鍵刪除。

---

## [v2.0.0] - 2026-07-30

### 🎉 現代側邊欄導覽與四大分頁架構發布

- 採用 `CustomTkinter` 側邊欄導覽列，區隔快取優化、開機加速、軟體卸載與設定白名單四大核心模組。

---

## [v1.3.0] - 2026-07-30

### 💡 Smart Cache Finder 智慧全碟快取自動盤點引擎

- 在 3~5 秒內全自動挖掘本機硬碟中大於 50 MB 之隱藏快取巨頭。

---

## [v1.2.0] - 2026-07-30

### 🚀 開源技術升級 (Win32 Working Set RAM 壓縮)

- 呼叫 Win32 `psapi.dll` `EmptyWorkingSet` 進行工作集壓縮，釋放背景閒置記憶體。

---

## [v1.1.0] - 2026-07-29

### 🌐 網頁快取與實時 RAM 監控升級

- 支援 Chrome、Edge、Brave、Firefox 快取清理與實時記憶體負載監控。

---

## [v1.0.0] - 2026-07-28

### 🎉 專案初始發布 (Initial Release)

- 基礎架構確立：Python 3 + CustomTkinter 深色介面與系統 Temp 清理。
